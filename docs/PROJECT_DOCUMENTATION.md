# Persona News — Документация проекта

## 1. Содержание

1. Содержание  
2. Введение  
3. Цель, задачи и суть проекта  
4. Подробное описание системы  
   4.1. Общая структура репозитория  
   4.2. Backend  
   4.3. Frontend  
   4.4. Инфраструктура и вспомогательные скрипты  
   4.5. Алгоритмы и ключевые процессы  
   4.6. Используемые библиотеки  
5. Примеры работы  
6. Заключение  
7. Список источников  
8. Примечание (описание файлов)

---

## 2. Введение

Persona News — это персонализированный новостной агрегатор, собирающий материалы с более чем 20 RSS‑источников, обрабатывающий их средствами машинного интеллекта (классификация, перевод, суммаризация) и предоставляющий удобный веб‑интерфейс для чтения и взаимодействия. Проект состоит из двух основных частей:

- **Backend (FastAPI, Celery, Redis, PostgreSQL/SQLite)** — отвечает за сбор данных, агрегацию, обработку и REST API.
- **Frontend (Next.js/React)** — обеспечивает интерактивный UI с фильтрами, сортировками, поиском и управлением статьями.

Проект поставляется со скриптом `start-dev.bat`, который поднимает все сервисы и обеспечивает автоматическую проверку доступности.

---

## 3. Цель, задачи и суть проекта

### Цель
Создать автоматизированную платформу, которая в реальном времени собирает новости, обогащает их метаданными и предоставляет пользователю персонализированную ленту с возможностью фильтрации и обратной связи.

### Задачи
- Сбор материала из RSS‑источников и кэширование результатов.
- Перевод и суммаризация текстов для унифицированного отображения.
- Тематическая классификация и извлечение сущностей.
- Предоставление REST API (`/feed`, `/sources`, `/feedback`, `/health`).
- Реализация фронтенда с фильтрами по темам, режимами просмотра и управлением избранным.
- Поддержка фоновых задач для регулярного обновления данных.

### Суть проекта
Persona News обеспечивает полный цикл обработки новостей — от получения RSS до визуализации в пользовательском интерфейсе. Пользователь видит 50 свежих материалов, может сортировать их (алфавит, свежесть, источник), фильтровать по темам, скрывать и сохранять статьи, а система будет учитывать обратную связь.

---

## 4. Подробное описание системы

### 4.1. Общая структура репозитория

```
Project/
├── backend/
│   ├── app/
│   │   ├── config.py
│   │   ├── main.py
│   │   ├── datasources/
│   │   │   └── rss.py
│   │   ├── services/
│   │   │   ├── feed_cache.py
│   │   │   ├── recommender.py
│   │   │   ├── summarizer.py
│   │   │   ├── topic_classifier.py
│   │   │   └── translator.py
│   │   ├── tasks/
│   │   │   └── ingest.py
│   │   ├── schemas.py
│   │   └── models.py
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── app/
│   │   ├── page.tsx
│   │   └── globals.css
│   ├── components/
│   │   ├── AppHeader.tsx
│   │   ├── FeedToolbar.tsx
+│   │   ├── HiddenPanel.tsx
│   │   ├── TopicCard.tsx
│   │   └── TopicFilter.tsx
│   ├── lib/api.ts
│   ├── package.json
│   └── Dockerfile
├── docs/
│   ├── ARCHITECTURE.md
│   ├── DATA_MODEL.md
│   ├── ROADMAP.md
│   └── PROJECT_DOCUMENTATION.md (данный документ)
├── docker-compose.yml
├── start-dev.bat
└── README.md
```

### 4.2. Backend

#### `app/main.py`
Настраивает FastAPI, CORS, создаёт конечные точки:
- `/feed`: возвращает до 100 персонализированных статей.
- `/sources`: статический список RSS‑источников.
- `/feedback`: приём действий пользователя (like/dislike/hide/undo_hide).
- `/health`: статус сервера.

Использует `FeedCache`, `SimpleRecommender`, хранит пользовательские предпочтения в `USER_FEEDBACK`.

#### `app/config.py`
Читает переменные окружения (URL базы данных, Redis, API ключи) и предоставляет их через Pydantic‑модель настроек.

#### `app/datasources/rss.py`
Описывает датаклассы RSS‑источников и функцию `fetch`, которая возвращает структурированные записи RSS. Содержит список `DEFAULT_SOURCES`.

#### `app/services/feed_cache.py`
Ключевой элемент:
- `FeedCache`: хранит статьи до 300 штук, повышая лимит на источник до 25.
- `_fetch_source`: параллельно обходит RSS, суммаризирует, переводит, нормализует изображения, классифицирует темы и извлекает сущности.
- `_proxy_image`: маршрутизирует изображения через CDN для HTTPS.
- `_extract_entities`: регулярное извлечение именованных сущностей.

Используется `ThreadPoolExecutor` для одновременной обработки источников.

#### `app/services/topic_classifier.py`
Алгоритм классификации:
- Словарь ключевых слов на тему.
- Подсчёт совпадений по тексту (title+summary).
- Возвращает до 3 тем или «Общее».

#### `app/services/summarizer.py`
Инкапсулирует Summarizer (правила или ML) для сокращения текста до 2 предложений.

#### `app/services/translator.py`
Заглушка/обертка над переводчиком (может вызывать API) для перевода заголовков и саммари на русский.

#### `app/services/recommender.py`
`SimpleRecommender` вычисляет рейтинг статей с учётом свежести, тональности и пользовательских реакций (like/dislike). Используется в `/feed` для персонального порядка.

#### `app/tasks/ingest.py`
Celery‑задача запускает обновление `FeedCache` по расписанию.

#### `app/schemas.py`
Pydantic‑модели (`Article`, `HiddenArticleInfo`, `Source`, `RecommendationResponse`, `HealthResponse`). Обеспечивают валидацию ответов API.

### 4.3. Frontend

#### `app/page.tsx`
- React‑компонента страницы.
- Состояния: `articles`, `selectedTopics`, `sortBy`, `viewMode`, `savedIds`, `hiddenRecords`.
- Загружает до 100 статей и отображает 50 (без фильтров).
- Сортировка: `home` (алфавит), `fresh` (по дате), `source`.
- Фильтры: поиск, выбор тем, скрытие/восстановление, избранное, переключение сетка/список.

#### Компоненты
- `AppHeader.tsx`: бренд, строка поиска, кнопка обновления, счётчики.
- `FeedToolbar.tsx`: сортировки, переключатель вида, отображение статистики (показано/всего).
- `TopicFilter.tsx`: список тем (Общее, Технологии, Кибербезопасность, Экономика, Общество, Культура, Наука, Спорт), выбор и сброс фильтров.
- `TopicCard.tsx`: карточка новости с изображением, темами, сущностями, кнопками like/dislike/save/hide.
- `HiddenPanel.tsx`: управление скрытыми статьями и таймером восстановления.

#### `lib/api.ts`
Функции работы с бекендом:
- `fetchFeed(limit)`: GET `/feed`.
- `fetchSources()`: GET `/sources`.
- `submitFeedback(articleId, action)`: POST `/feedback`.
Включает нормализацию базового URL API и обработку сетевых ошибок.

#### `app/globals.css`
Глобальные стили: сетка карточек, адаптивность, цвета состояний свежести, оформление фильтров, панели скрытых статей, шапки приложения, тем оформления.

### 4.4. Инфраструктура и вспомогательные скрипты

- `docker-compose.yml`: поднимает backend, frontend, Redis, PostgreSQL (если требуется).
- `backend/Dockerfile`, `frontend/Dockerfile`: сборка сервисов.
- `start-dev.bat`: автоматический запуск локально.
  1. Проверяет/создаёт виртуальное окружение backend.
  2. Ставит зависимости.
  3. Проверяет `node_modules` для frontend.
  4. Запускает backend (uvicorn) и frontend (Next.js) в отдельных окнах.
  5. Пингует `/health`, информирует пользователя.

### 4.5. Алгоритмы и ключевые процессы

1. **Сбор RSS**: `FeedCache._fetch_source`
   - `fetch()` → парсинг → очистка → суммаризация → перевод → классификация → извлечение сущностей → нормализация изображений.
   - Использует потоковый пул для параллельной загрузки.

2. **Кэширование**: `FeedCache.get_articles()`
   - TTL 10 минут, максимум 300 записей.
   - При устаревании — заново собирает всё.

3. **Персонализация**: `SimpleRecommender.recommend`
   - Комбинация свежести, тональности, прошлых реакций.
   - Результат сортируется перед срезом `[:limit]`.

4. **Управление скрытыми статьями**:
   - `USER_FEEDBACK` хранит скрытые ID на 1 час.
   - `/feed` исключает их, `/feedback?action=undo_hide` возвращает.

5. **Frontend‑сортировка**:
   - `home`: `localeCompare` по заголовкам (50 статей, алфавит).
   - `fresh`: сравнение дат публикации.
   - `source`: `localeCompare` по названию источника.

6. **Фильтрация по темам и поиску**:
   - `baseVisibleArticles`: проверка скрытых и строка поиска.
   - `selectedTopics`: отображает статьи с выбранными темами, добавляет связанные при недостатке.

7. **Избранное и скрытие на клиенте**:
   - `savedIds` хранится в `localStorage`.
   - `HiddenPanel` показывает список скрытых с таймером и кнопкой восстановления, отправляет `submitFeedback` на backend.

### 4.6. Используемые библиотеки

- Backend: `fastapi`, `uvicorn`, `pydantic`, `sqlalchemy`, `asyncpg`, `redis`, `celery`, `httpx`, `feedparser`, `beautifulsoup4`, `numpy`, `deep-translator`.
- Frontend: `next`, `react`, `typescript`, `node-fetch` (через встроенный fetch), CSS‑модули.
- Вспомогательные: `python-dotenv`, `typing-inspection`, `annotated-types`.

---

## 5. Примеры работы

1. **Загрузка ленты**
   - Пользователь открывает сайт → `page.tsx` вызывает `fetchFeed(100)` → отображает 50 статей в алфавитном порядке («Главная»).
2. **Сортировка**
   - Переключение на «Сначала свежие» вызывает `setSortBy("fresh")`, UI пересортировывает статьи по дате.
3. **Фильтр по темам**
   - Нажатие на тему «Технологии» — отображаются статьи, у которых в `article.topics` есть эта тема.
4. **Скрытие/восстановление**
   - Кнопка «👁‍🗨 скрыть» → `submitFeedback(action="hide")` → статья исчезает и уходит в панель Hidden.
   - В панели «Скрытые» кнопка «Вернуть» вызывает `submitFeedback(action="undo_hide")`.
5. **API пример**
   - `GET http://localhost:8000/feed?user_id=demo&limit=50` возвращает JSON:
     ```json
     {
       "user_id": "demo",
       "generated_at": "...",
       "articles": [
         {
           "id": 123456,
           "title": "Новый релиз AI",
           "summary": "...",
           "topics": ["Технологии", "Наука"],
           "source_name": "TechCrunch",
           "image_url": "https://images.weserv.nl/?url=..."
         }
       ],
       "hidden": []
     }
     ```

---

## 6. Заключение

Persona News демонстрирует полный цикл создания современного новостного агрегатора: от получения RSS‑потоков до адаптивного пользовательского интерфейса, предоставляющего персональные рекомендации. Важной особенностью является баланс между инженерной строгостью и гибкостью: параллельный сбор и кэширование статей, интеллектуальная обработка текстов, а также мгновенное обновление интерфейса обеспечивают стабильно высокое качество ленты даже при увеличении количества источников.

Благодаря модульной архитектуре проект легко масштабируется и дорабатывается. На бекенде можно расширять перечень RSS‑каналов, экспериментировать с более продвинутыми алгоритмами категоризации и суммаризации, внедрять ML‑рекомендации или коннекторы к внешним API. На фронтенде просто добавлять новые сценарии взаимодействия, вводить пользовательские профили, систему подписок или push‑уведомлений. Существующий стек (FastAPI + Celery + Next.js) облегчает деплой в Docker‑средах и интеграцию с облачными сервисами, что делает проект готовой основой для коммерческого развития или внутренних аналитических инструментов.

В перспективе Persona News может превратиться в полноценную медиаплатформу: добавить аутентификацию, рейтинг источников, полнотекстовый поиск, расширенную аналитику поведения пользователей и интеграцию с социальной активностью. Текущая реализация уже обеспечивает устойчивую, гибкую и хорошо документированную базу для дальнейших экспериментов и внедрений.

---

## 7. Список источников

1. RSS‑ленты: BBC World, CNN World, Reuters, Guardian, Euronews, Deutsche Welle, Meduza, Lenta, RBC, Bloomberg, CNBC, Business Insider, AP News, TechCrunch, Wired, The Verge, Ars Technica, Engadget, Wired Security, N+1, MIT Tech Review.
2. Документация:
   - FastAPI — https://fastapi.tiangolo.com  
   - Next.js — https://nextjs.org/docs  
   - Celery — https://docs.celeryq.dev  
3. Библиотеки: feedparser, BeautifulSoup, Pydantic, React, TypeScript (официальные ресурсы).

---

## 8. Примечание (описание файлов)

| Файл | Содержимое / назначение |
|------|------------------------|
| `backend/app/main.py` | `import fastapi`, определение маршрутов `/feed`, `/sources`, `/feedback`, `/health`; запуск приложения. |
| `backend/app/config.py` | Импорт `pydantic`, чтение переменных окружения, функция `get_settings()`. |
| `backend/app/datasources/rss.py` | `feedparser.parse`, `DEFAULT_SOURCES`, функция `fetch` с ограничением по количеству записей. |
| `backend/app/services/feed_cache.py` | Импорт `hashlib`, `ThreadPoolExecutor`, `BeautifulSoup`. Методы `_fetch_source`, `_proxy_image`, `get_articles`, класс `FeedCache`. |
| `backend/app/services/topic_classifier.py` | Словарь `TOPIC_KEYWORDS`, функция `classify` и `batch_classify`. |
| `backend/app/services/summarizer.py` | Класс `Summarizer`, логика сокращения текста до 2 предложений. |
| `backend/app/services/translator.py` | Функции перевода текста (заглушка/вызов API), нормализация кодировки. |
| `backend/app/services/recommender.py` | Класс `SimpleRecommender`, методы `recommend`, `add_feedback`. |
| `backend/app/tasks/ingest.py` | Импорт `celery`, задача `ingest_sources`. |
| `backend/app/schemas.py` | Pydantic‑модели `Article`, `Source`, `RecommendationResponse`, `HiddenArticleInfo`. |
| `backend/requirements.txt` | `fastapi==0.115.2`, `uvicorn`, `pydantic==2.12.4`, `celery`, `redis`, `feedparser`, `beautifulsoup4` и др. |
| `frontend/app/page.tsx` | Импорт React‑хуков, компонентов, `fetchFeed`. Реализация логики загрузки, сортировки, фильтрации, отображения 50 статей. |
| `frontend/app/globals.css` | Стили для hero‑блока, карточек, фильтров, панели скрытых статей, адаптивности. |
| `frontend/components/AppHeader.tsx` | JSX для шапки, поле поиска, кнопка обновления. |
| `frontend/components/FeedToolbar.tsx` | Кнопки сортировки («Главная», «Сначала свежие», «По источнику»), переключатель вида. |
| `frontend/components/TopicFilter.tsx` | Отрисовка кнопок тем, кнопка «Сбросить», обработчик `onToggleTopic`. |
| `frontend/components/TopicCard.tsx` | Карточка новости: изображение, заголовок, summary, тэги, кнопки like/dislike/save/hide. |
| `frontend/components/HiddenPanel.tsx` | Список скрытых статей, таймер до автоматического возвращения, кнопка «Вернуть». |
| `frontend/lib/api.ts` | `fetchFeed`, `fetchSources`, `submitFeedback`, обработка URL и ошибок. |
| `frontend/package.json` | Зависимости Next.js, React, TypeScript, скрипты `dev`, `build`, `lint`. |
| `start-dev.bat` | Создание виртуального окружения, установка зависимостей, запуск backend/frontend, проверка `/health`. |
| `docker-compose.yml` | Сервисы `backend`, `frontend`, при необходимости `redis` и БД, настройки портов. |
| `docs/ARCHITECTURE.md`, `DATA_MODEL.md`, `ROADMAP.md` | Архитектура, модели данных, дорожная карта. |

> Примечание: описание охватывает файлы, непосредственно задействованные в работе приложения. Дополнительные файлы (например, `.env.example`, тесты, конфигурация CI) включать по мере появления.

---

Документ можно использовать как базу для пользовательской и технической документации, дополняя примерами, скриншотами и инструкциями по деплою.


